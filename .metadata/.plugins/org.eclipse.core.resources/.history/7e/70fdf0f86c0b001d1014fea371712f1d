package bigJumpTest;

public class TestesBigJump {

	public static int jumps(int flagHeight, int bigJump) {
		if (bigJump == 1) {
			return flagHeight / bigJump;
		} else {
			int parcial = flagHeight / bigJump;
			int rest = flagHeight % bigJump;
			int resultado = parcial + rest;
			return resultado;
		}

	}

}
